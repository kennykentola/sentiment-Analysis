import PlaceholderPage from '@/components/ui/PlaceholderPage';

export default function DataSources() {
  return (
    <PlaceholderPage 
      title="Data Sources" 
      description="Manage and monitor data ingestion streams and API connections." 
    />
  );
}
