import PlaceholderPage from '@/components/ui/PlaceholderPage';

export default function Notifications() {
  return (
    <PlaceholderPage 
      title="Notifications" 
      description="View alerts, system updates, and scheduled job statuses." 
    />
  );
}
